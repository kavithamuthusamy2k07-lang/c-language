#include<stdio.h>
int main()
{
    char ch;
    printf("enter the ch:");
    scanf("%c",&ch);
    if (ch>=65&&ch<=90)
    {

        printf("%c:upper case",ch);
    }
    else if(ch>=97&&ch<=122)
    {
        printf("%c:lower case",ch);
    }
     return 0;
    }
